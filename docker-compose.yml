services:
  postgres:
    image: postgres:16-alpine
    container_name: chamnhatban_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-chamnhatban}
      POSTGRES_USER: ${POSTGRES_USER:-chamnhatban}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chamnhatban_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-chamnhatban} -d ${POSTGRES_DB:-chamnhatban}"
        ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
